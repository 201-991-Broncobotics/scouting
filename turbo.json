{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "build": {
      "outputs": [".next/**", "dist/**", "build/**"],
      "dependsOn": ["lint", "check", "^check", "^lint"]
    },
    "lint": {},
    "check": {}
  }
}
